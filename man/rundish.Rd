% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/celltypes.R
\name{rundish}
\alias{rundish}
\title{Estimate cell type proportions}
\usage{
rundish(betas, ref = NULL, method = NULL, logger = NULL)
}
\arguments{
\item{betas}{probes-by-samples beta matrix.}

\item{ref}{reference panel; see \code{mqcdefaults()} for the valid names.}

\item{method}{deconvolution method passed to EpiDISH.}

\item{logger}{optional logger.}
}
\value{
a data.frame with \code{sample_id} and one column per cell type, or
  \code{NULL} if EpiDISH is unavailable or nothing matched.
}
\description{
Wraps \pkg{EpiDISH}. Reference probe sets are keyed on bare \code{cg}
identifiers, so EPICv2 data must have its replicate suffixes resolved
first (see \code{\link{collapsev2}}); otherwise no reference probe matches.
}
\examples{
\dontrun{
props <- rundish(betas, ref = "blood")
}
}
