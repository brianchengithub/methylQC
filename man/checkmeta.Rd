% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/columns.R
\name{checkmeta}
\alias{checkmeta}
\title{Validate that a sample sheet has the columns downstream stages need}
\usage{
checkmeta(ss, logger = NULL)
}
\arguments{
\item{ss}{sample sheet.}

\item{logger}{optional logger.}
}
\value{
a list naming the resolved columns, invisibly.
}
\description{
Each column is matched on name and then confirmed against its own values,
so a sheet with unexpected headers still resolves and a misleading header is
not taken at face value.
}
\examples{
ss <- data.frame(Fname = c("a", "b", "c"), Age = c(30, 40, 50),
                 Sex = c("M", "F", "F"), Donor = c("d1", "d2", "d3"))
checkmeta(ss)
}
