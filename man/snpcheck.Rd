% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/celltypes.R
\name{snpcheck}
\alias{snpcheck}
\title{Check donor identity using SNP probes}
\usage{
snpcheck(snps, donors = NULL, minmatch = 0.9, keepmin = NULL, logger = NULL)
}
\arguments{
\item{snps}{rs-probe beta matrix from \code{\link{snpbetas}}.}

\item{donors}{optional named vector mapping sample IDs to donor IDs.}

\item{minmatch}{concordance above which two samples are called identical.}

\item{keepmin}{concordance below which an unrelated pair is not reported.}

\item{logger}{optional logger.}
}
\value{
a data.frame of sample pairs with their concordance, or \code{NULL}.
}
\description{
Genotypes are called by thresholding the rs beta values into three classes
and compared pairwise. Pairs at or above \code{minmatch} are reported as the
same individual.
}
\details{
The comparison is quadratic in samples, so at cohort scale the full pair
list is not returned: only pairs that are interesting are kept, meaning
those at or above \code{keepmin} concordance plus every pair the donor
labels say should match. A swap shows up in the second group -- a pair
expected to match that does not -- so filtering on concordance alone would
hide exactly the case worth finding.
}
\examples{
\dontrun{
pairs <- snpcheck(snpbetas(betas))
}
}
