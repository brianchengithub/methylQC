% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{mdsoutlier}
\alias{mdsoutlier}
\title{Flag samples far from the cohort centroid in MDS space}
\usage{
mdsoutlier(mds, nsd = NULL)
}
\arguments{
\item{mds}{a two-column MDS coordinate matrix.}

\item{nsd}{distance, in SDs, beyond which a sample is an outlier.}
}
\value{
a named logical vector, with the radius as an attribute.
}
\description{
Distance is measured from the coordinate-wise median, which is unaffected by
the outliers it is meant to find, and scaled by the median absolute
deviation of those distances.
}
\examples{
m <- cbind(MDS1 = c(0, 0.1, -0.1, 5), MDS2 = c(0, -0.1, 0.1, 5))
rownames(m) <- paste0("s", 1:4)
mdsoutlier(m)
}
