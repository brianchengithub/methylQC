% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qcplan.R
\name{qcplan}
\alias{qcplan}
\title{Preflight a methylQC run}
\usage{
qcplan(
  indir = NULL,
  ss = NULL,
  platform = NULL,
  memfrac = NULL,
  extreme = NULL,
  savesdf = NULL,
  outdir = NULL,
  measure = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{indir}{directory of IDAT files, or \code{NULL} if \code{ss} is given.}

\item{ss}{an already-discovered sample sheet, to avoid re-scanning.}

\item{platform}{platform string; inferred when \code{NULL}.}

\item{memfrac}{fraction of available memory the run may use.}

\item{extreme}{plan for extreme memory conservation mode.}

\item{savesdf}{whether SigDF retention is wanted; the plan may withdraw it.}

\item{outdir}{destination directory, used only to check free disk space.}

\item{measure}{if \code{FALSE}, skip processing a sample and use a
conservative default for per-sample cost.}

\item{verbose}{print the report.}
}
\value{
a list with \code{workers}, \code{batch}, \code{savesdf},
  \code{n_samples}, \code{n_probes}, \code{per_sample}, \code{parent_bytes},
  \code{projected_peak}, \code{memcap}, \code{parentcap}, \code{fits} and
  \code{text}.
}
\description{
Measures the actual per-sample memory cost on this machine with this array,
reads available system memory, and recommends a worker count, a batch size
and whether the preprocessed \code{SigDF} objects can be retained.
}
\details{
\code{qc()} calls this automatically when \code{workers} and \code{batch}
are not given, so the defaults are self-derived. Any argument you pass to
\code{qc()} overrides the corresponding recommendation.
}
\examples{
\dontrun{
plan <- qcplan("~/idats")
qc("~/idats", "~/qcout", workers = plan$workers)
}
}
