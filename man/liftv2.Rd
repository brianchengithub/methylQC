% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epicv2.R
\name{liftv2}
\alias{liftv2}
\title{De-duplicate EPICv2 replicate probes and harmonise probe IDs}
\usage{
liftv2(
  mat = NULL,
  detP = NULL,
  matpath = NULL,
  detppath = NULL,
  platform = c("EPIC", "HM450"),
  pthresh = NULL,
  dedupcsv = NULL,
  details = FALSE,
  logger = NULL
)
}
\arguments{
\item{mat}{Numeric matrix (probes x samples): a beta-value or
M-value matrix with EPICv2 probe IDs as row names. May be supplied
directly or via \code{matpath}.}

\item{detP}{Numeric matrix (probes x samples) of detection p-values,
same row/column names as \code{mat}. May be supplied via
\code{detppath}. Required: the de-duplication criterion is the
cross-sample detection failure rate.}

\item{matpath}{Optional path to an .rds file holding \code{mat}.}

\item{detppath}{Optional path to an .rds file holding \code{detP}.}

\item{platform}{Target platform, \code{"EPIC"} (default) or
\code{"HM450"}.}

\item{pthresh}{Detection p-value threshold defining a "failure"
(default: \code{mqcopts()$detp}, 0.05).}

\item{dedupcsv}{Optional path to write the per-CpG kept/dropped log
as CSV.}

\item{details}{If TRUE, return a list with the harmonised matrix
plus the probe-ID correspondence needed to re-align companion
matrices. If FALSE (default), return just the harmonised matrix.}

\item{logger}{Optional logger.}
}
\value{
If \code{details = FALSE}, the de-duplicated, ID-harmonised
matrix (probes x samples). If TRUE, a list with \code{mat},
\code{kept_ids}, \code{id_map}, and \code{platform}.
}
\description{
Cohort-consistent replicate resolution followed by probe ID
harmonisation to a legacy Infinium platform via SeSAMe's
\code{mLiftOver}.
}
\examples{
\dontrun{
# Matrices produced by methylQC Stage 1 (betas_all.rds is unmasked,
# detP_all.rds holds pOOBAH detection p-values; both are
# probes x samples with probe IDs as row names).
harmonised <- liftv2(
  matpath  = "results/betas_all.rds",
  detppath = "results/detP_all.rds",
  platform = "EPIC",
  dedupcsv = "results/epicv2_dedup_log.csv")
}
}
