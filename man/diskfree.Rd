% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{diskfree}
\alias{diskfree}
\title{Free space on the filesystem holding a path}
\usage{
diskfree(path)
}
\arguments{
\item{path}{a path on the filesystem to query; need not exist yet, the
nearest existing parent is used.}
}
\value{
free bytes, or \code{NA_real_} if the probe fails.
}
\description{
Used to guard the \code{sdfs_all.rds} write, which is the largest single
artefact the pipeline produces. \code{df -Pk} is POSIX and behaves
identically on macOS and Linux, so no new dependency is needed.
}
\examples{
diskfree(tempdir())
}
